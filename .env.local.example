# Copy this file to .env.local and fill in values
# The backend URL is configured in next.config.mjs as a proxy rewrite.
# Change it there if your backend moves.

# Optional: override the backend URL via env (requires updating next.config.mjs to read this)
NEXT_PUBLIC_BACKEND_URL=http://3.108.220.238:13000
